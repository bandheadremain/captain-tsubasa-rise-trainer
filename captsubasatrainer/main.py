[
    {
        "path": "captain-tsubasa-rise-trainer/requirements.txt",
        "content": "pypiwin32==223\npsutil==5.9.5\nkeyboard==0.13.5\npymem==1.0.1\nrequests==2.31.0\n"
    },
    {
        "path": "captain-tsubasa-rise-trainer/src/__init__.py",
        "content": ""
    },
    {
        "path": "captain-tsubasa-rise-trainer/src/memory_reader.py",
        "content": "import pymem\nimport pymem.process\nfrom typing import Optional, Dict, Any\n\n\nclass MemoryReader:\n    \"\"\"\n    Handles reading and writing to the Captain Tsubasa: Rise of New Champions process memory.\n    Uses pymem to interface with the game's memory addresses.\n    \"\"\"\n\n    def __init__(self, process_name: str = \"TsubasaRiseOfNewChampions.exe\"):\n        self.process_name = process_name\n        self.pm: Optional[pymem.Pymem] = None\n        self._base_address: Optional[int] = None\n\n    def attach(self) -> bool:\n        \"\"\"Attach to the game process.\"\"\"\n        try:\n            self.pm = pymem.Pymem(self.process_name)\n            self._base_address = pymem.process.module_from_name(\n                self.pm.process_handle, self.process_name\n            ).lpBaseOfDll\n            return True\n        except pymem.exception.ProcessNotFound:\n            return False\n        except Exception:\n            return False\n\n    def is_attached(self) -> bool:\n        \"\"\"Check if we are attached to the game process.\"\"\"\n        return self.pm is not None\n\n    def read_int(self, address: int) -> Optional[int]:\n        \"\"\"Read a 4-byte integer from the given address.\"\"\"\n        if not self.is_attached():\n            return None\n        try:\n            return self.pm.read_int(address)\n        except Exception:\n            return None\n\n    def write_int(self, address: int, value: int) -> bool:\n        \"\"\"Write a 4-byte integer to the given address.\"\"\"\n        if not self.is_attached():\n            return False\n        try:\n            self.pm.write_int(address, value)\n            return True\n        except Exception:\n            return False\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a 4-byte float from the given address.\"\"\"\n        if not self.is_attached():\n            return None\n        try:\n            return self.pm.read_float(address)\n        except Exception:\n            return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"Write a 4-byte float to the given address.\"\"\"\n        if not self.is_attached():\n            return False\n        try:\n            self.pm.write_float(address, value)\n            return True\n        except Exception:\n            return False\n\n    def read_bytes(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read a sequence of bytes from the given address.\"\"\"\n        if not self.is_attached():\n            return None\n        try:\n            return self.pm.read_bytes(address, size)\n        except Exception:\n            return None\n\n    def write_bytes(self, address: int, data: bytes) -> bool:\n        \"\"\"Write a sequence of bytes to the given address.\"\"\"\n        if not self.is_attached():\n            return False\n        try:\n            self.pm.write_bytes(address, data)\n            return True\n        except Exception:\n            return False\n\n    @property\n    def base_address(self) -> Optional[int]:\n        \"\"\"Get the base address of the game module.\"\"\"\n        return self._base_address\n\n    def close(self):\n        \"\"\"Close the memory handle.\"\"\"\n        if self.pm:\n            self.pm.close_process()\n            self.pm = None\n"
    },
    {
        "path": "captain-tsubasa-rise-trainer/src/trainer_core.py",
        "content": "import time\nimport threading\nfrom typing import Dict, Any, Callable, Optional\nfrom .memory_reader import MemoryReader\n\n\nclass TrainerCore:\n    \"\"\"\n    Core trainer functionality for Captain Tsubasa: Rise of New Champions.\n    Manages toggles, hotkeys, and memory modifications.\n    \"\"\"\n\n    # Example memory offsets (these are illustrative; real offsets would require reverse engineering)\n    OFFSETS = {\n        \"spirit\": 0x00A1B2C0,       # Player spirit value\n        \"drive_gauge\": 0x00A1B2C4,  # Special drive gauge\n        \"stamina\": 0x00A1B2C8,      # Player stamina\n        \"score\": 0x00A1B2CC,        # Current match score\n        \"time\": 0x00A1B2D0,         # Match timer (seconds)\n        \"speed_mult\": 0x00A1B2D4,   # Game speed multiplier\n    }\n\n    def __init__(self):\n        self.memory = MemoryReader()\n        self._running = False\n        self._thread: Optional[threading.Thread] = None\n        self._toggles: Dict[str, bool] = {\n            \"infinite_spirit\": False,\n            \"infinite_stamina\": False,\n            \"max_drive_gauge\": False,\n            \"freeze_time\": False,\n            \"speed_hack\": False,\n        }\n        self._hotkeys: Dict[str, str] = {\n            \"infinite_spirit\": \"F1\",\n            \"infinite_stamina\": \"F2\",\n            \"max_drive_gauge\": \"F3\",\n            \"freeze_time\": \"F4\",\n            \"speed_hack\": \"F5\",\n        }\n        self._speed_multiplier: float = 1.0\n        self._original_values: Dict[str, Any] = {}\n\n    def attach(self) -> bool:\n        \"\"\"Attach to the game process.\"\"\"\n        return self.memory.attach()\n\n    def detach(self):\n        \"\"\"Detach from the game process.\"\"\"\n        self.stop()\n        self.memory.close()\n\n    def start(self):\n        \"\"\"Start the trainer loop in a background thread.\"\"\"\n        if self._running:\n            return\n        self._running = True\n        self._thread = threading.Thread(target=self._loop, daemon=True)\n        self._thread.start()\n\n    def stop(self):\n        \"\"\"Stop the trainer loop.\"\"\"\n        self._running = False\n        if self._thread:\n            self._thread.join(timeout=1.0)\n            self._thread = None\n\n    def _loop(self):\n        \"\"\"Main trainer loop applying memory modifications.\"\"\"\n        while self._running:\n            if not self.memory.is_attached():\n                time.sleep(0.5)\n                continue\n\n            base = self.memory.base_address\n            if base is None:\n                time.sleep(0.5)\n                continue\n\n            try:\n                if self._toggles.get(\"infinite_spirit\", False):\n                    self.memory.write_int(base + self.OFFSETS[\"spirit\"], 9999)\n\n                if self._toggles.get(\"infinite_stamina\", False):\n                    self.memory.write_int(base + self.OFFSETS[\"stamina\"], 100)\n\n                if self._toggles.get(\"max_drive_gauge\", False):\n                    self.memory.write_float(base + self.OFFSETS[\"drive_gauge\"], 100.0)\n\n                if self._toggles.get(\"freeze_time\", False):\n                    self.memory.write_int(base + self.OFFSETS[\"time\"], 600)  # 10 minutes\n\n                if self._toggles.get(\"speed_hack\", False):\n                    self.memory.write_float(base + self.OFFSETS[\"speed_mult\"], self._speed_multiplier)\n\n            except Exception:\n                pass  # Memory read/write errors are silently ignored\n\n            time.sleep(0.05)  # ~20 Hz update rate\n\n    def toggle_feature(self, feature_name: str) -> Optional[bool]:\n        \"\"\"Toggle a trainer feature on/off. Returns new state or None if invalid.\"\"\"\n        if feature_name not in self._toggles:\n            return None\n        self._toggles[feature_name] = not self._toggles[feature_name]\n        return self._toggles[feature_name]\n\n    def set_feature(self, feature_name: str, state: bool) -> bool:\n        \"\"\"Set a feature to a specific state.\"\"\"\n        if feature_name not in self._toggles:\n            return False\n        self._toggles[feature_name] = state\n        return True\n\n    def get_feature_state(self, feature_name: str) -> Optional[bool]:\n        \"\"\"Get the current state of a feature.\"\"\"\n        return self._toggles.get(feature_name)\n\n    def set_speed_multiplier(self, multiplier: float):\n        \"\"\"Set the game speed multiplier (0.1 to 5.0).\"\"\"\n        self._speed_multiplier = max(0.1, min(5.0, multiplier))\n\n    def get_speed_multiplier(self) -> float:\n        \"\"\"Get current speed multiplier.\"\"\"\n        return self._speed_multiplier\n\n    def get_hotkey(self, feature_name: str) -> Optional[str]:\n        \"\"\"Get the hotkey for a given feature.\"\"\"\n        return self._hotkeys.get(feature_name)\n\n    def set_hotkey(self, feature_name: str, key: str) -> bool:\n        \"\"\"Set a new hotkey for a feature.\"\"\"\n        if feature_name not in self._hotkeys:\n            return False\n        self._hotkeys[feature_name] = key\n        return True\n\n    def get_all_states(self) -> Dict[str, Any]:\n        \"\"\"Get all trainer states as a dictionary.\"\"\"\n        return {\n            \"features\": dict(self._toggles),\n            \"speed_multiplier\": self._speed_multiplier,\n            \"hotkeys\": dict(self._hotkeys),\n            \"attached\": self.memory.is_attached(),\n        }\n"
    },
    {
        "path": "captain-tsubasa-rise-trainer/src/hotkey_listener.py",
        "content": "import keyboard\nimport threading\nfrom typing import Dict, Callable, Optional\n\n\nclass HotkeyListener:\n    \"\"\"\n    Listens for global hotkeys and triggers callbacks.\n    Uses the 'keyboard' library for low-level key hooks.\n    \"\"\"\n\n    def __init__(self):\n        self._callbacks: Dict[str, Callable] = {}\n        self._hotkeys: Dict[str, str] = {}\n        self._running = False\n        self._thread: Optional[threading.Thread] = None\n\n    def register_hotkey(self, hotkey: str, callback: Callable):\n        \"\"\"Register a hotkey with an associated callback.\"\"\"\n        self._hotkeys[hotkey] = callback\n        if self._running:\n            keyboard.add_hotkey(hotkey, callback)\n\n    def unregister_hotkey(self, hotkey: str):\n        \"\"\"Unregister a hotkey.\"\"\"\n        if hotkey in self._hotkeys:\n            del self._hotkeys[hotkey]\n            if self._running:\n                keyboard.remove_hotkey(hotkey)\n\n    def start(self):\n        \"\"\"Start listening for hotkeys in a background thread.\"\"\"\n        if self._running:\n            return\n        self._running = True\n        for hotkey, callback in self._hotkeys.items():\n            keyboard.add_hotkey(hotkey, callback)\n        self._thread = threading.Thread(target=self._listen, daemon=True)\n        self._thread.start()\n\n    def stop(self):\n        \"\"\"Stop listening for hotkeys.\"\"\"\n        self._running = False\n        keyboard.unhook_all()\n        if self._thread:\n            self._thread.join(timeout=1.0)\n            self._thread = None\n\n    def _listen(self):\n        \"\"\"Internal loop to keep the listener alive.\"\"\"\n        while self._running:\n            # keyboard library handles hotkeys via hooks; this just keeps thread alive\n            threading.Event().wait(0.1)\n\n    def is_running(self) -> bool:\n        \"\"\"Check if the listener is active.\"\"\"\n        return self._running\n\n    def clear_all(self):\n        \"\"\"Remove all registered hotkeys.\"\"\"\n        self.stop()\n        self._hotkeys.clear()\n        self._callbacks.clear()\n"
    },
    {
        "path": "captain-tsubasa-rise-trainer/main.py",
        "content": "import sys\nimport time\nfrom src.trainer_core import TrainerCore\nfrom src.hotkey_listener import HotkeyListener\n\n\ndef main():\n    \"\"\"\n    Main entry point for the Captain Tsubasa: Rise of New Champions Trainer.\n    Initializes the trainer core and hotkey listener, then waits for user input.\n    \"\"\"\n    print(\"Captain Tsubasa: Rise of New Champions Trainer\")\n    print(\"=\" * 50)\n    print(\"Waiting for game process...\")\n\n    trainer = TrainerCore()\n    listener = HotkeyListener()\n\n    # Attach to game\n    if not trainer.attach():\n        print(\"Error: Could not find game process. Please start the game first.\")\n        sys.exit(1)\n\n    print(\"Game found! Attached successfully.\")\n    print(\"\\nHotkeys:\")\n    print(\"  F1 - Toggle Infinite Spirit\")\n    print(\"  F2 - Toggle Infinite Stamina\")\n    print(\"  F3 - Toggle Max Drive Gauge\")\n    print(\"  F4 - Toggle Freeze Time\")\n    print(\"  F5 - Toggle Speed Hack (default 2x)\")\n    print(\"  F6 - Increase Speed\")\n    print(\"  F7 - Decrease Speed\")\n    print(\"  F8 - Show Status\")\n    print(\"  F9 - Exit\")\n    print(\"\\n\" + \"=\" * 50)\n\n    # Define hotkey callbacks\n    def toggle_infinite_spirit():\n        state = trainer.toggle_feature(\"infinite_spirit\")\n        print(f\"Infinite Spirit: {'ON' if state else 'OFF'}\")\n\n    def toggle_infinite_stamina():\n        state = trainer.toggle_feature(\"infinite_stamina\")\n        print(f\"Infinite Stamina: {'ON' if state else 'OFF'}\")\n\n    def toggle_max_drive():\n        state = trainer.toggle_feature(\"max_drive_gauge\")\n        print(f\"Max Drive Gauge: {'ON' if state else 'OFF'}\")\n\n    def toggle_freeze_time():\n        state = trainer.toggle_feature(\"freeze_time\")\n        print(f\"Freeze Time: {'ON' if state else 'OFF'}\")\n\n    def toggle_speed_hack():\n        state = trainer.toggle_feature(\"speed_hack\")\n        print(f\"Speed Hack: {'ON' if state else 'OFF'}\")\n        if state:\n            trainer.set_speed_multiplier(2.0)\n\n    def increase_speed():\n        current = trainer.get_speed_multiplier()\n        new_speed = min(5.0, current + 0.5)\n        trainer.set_speed_multiplier(new_speed)\n        print(f\"Speed: {new_speed:.1f}x\")\n\n    def decrease_speed():\n        current = trainer.get_speed_multiplier()\n        new_speed = max(0.5, current - 0.5)\n        trainer.set_speed_multiplier(new_speed)\n        print(f\"Speed: {new_speed:.1f}x\")\n\n    def show_status():\n        states = trainer.get_all_states()\n        print(\"\\n--- Trainer Status ---\")\n        print(f\"Attached: {states['attached']}\")\n        for feature, enabled in states['features'].items():\n            print(f\"  {feature}: {'ON' if enabled else 'OFF'}\")\n        print(f\"Speed Multiplier: {states['speed_multiplier']:.1f}x\")\n        print(\"-\" * 22)\
